'use strict';

const Promise = require('bluebird');

const ThunderSDK = require('./lib/thunder-sdk');

Promise.config({
  cancellation: true
});

var defaultInstance = new ThunderSDK('mqtt://localhost');

defaultInstance.ThunderSDK = ThunderSDK;
module.exports = defaultInstance;
